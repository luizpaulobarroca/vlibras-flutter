---
phase: 03-web-platform-integration
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - web/vlibras/vlibras.js
autonomous: false
requirements: [WEB-01, WEB-02, CORE-02]

user_setup:
  - service: vlibras-player-webjs
    why: "The pre-compiled vlibras.js bundle must be obtained from the vlibras-player-webjs repository (UFPB/LAVID). It requires Node.js + npm to build from source."
    env_vars: []
    dashboard_config:
      - task: "Build vlibras.js from source"
        location: "Clone https://github.com/spbgovbr-vlibras/vlibras-player-webjs, run 'npm install && npm run build', copy the output bundle to web/vlibras/vlibras.js in the plugin"
      - task: "Obtain Unity WebGL assets"
        location: "The 'target/' directory with Unity WebGL assets is distributed separately. Place it at web/vlibras/target/ in your Flutter app (not in the plugin). Check the VLibras project documentation or contact UFPB/LAVID."

must_haves:
  truths:
    - "web/vlibras/vlibras.js exists in the plugin repository and exposes window.VLibras.Player in browser"
    - "A Flutter Web app with the script tag and target/ assets renders the VLibras avatar visibly"
    - "Calling controller.translate('Ola mundo') causes the avatar to animate LIBRAS signs"
    - "State transitions idle -> initializing -> ready -> translating -> playing -> ready are visible via ValueListenableBuilder in a demo app"
  artifacts:
    - path: "web/vlibras/vlibras.js"
      provides: "Pre-compiled vlibras-player-webjs bundle (committed to plugin web/ directory)"
  key_links:
    - from: "web/vlibras/vlibras.js"
      to: "window.VLibras.Player"
      via: "script tag in app index.html"
      pattern: "VLibras\\.Player"
---

<objective>
Commit the vlibras.js bundle to the plugin's web/ directory and perform human verification that the end-to-end flow works in a real browser: avatar renders, translate animates, state transitions are observable.

Purpose: Plans 01 and 02 built the Dart integration; this plan closes the loop with the real JS player asset and confirms the complete value proposition works.
Output: web/vlibras/vlibras.js committed; human-verified avatar animation and state transitions.
</objective>

<execution_context>
@C:/Users/Luiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-platform-integration/03-CONTEXT.md
@.planning/phases/03-web-platform-integration/03-01-SUMMARY.md
@.planning/phases/03-web-platform-integration/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web/vlibras/ directory and commit vlibras.js placeholder</name>
  <files>web/vlibras/vlibras.js</files>
  <action>
Create the directory structure `web/vlibras/` in the plugin root. This directory holds `vlibras.js` (committed with the plugin) and establishes the `/vlibras/` URL prefix convention from CONTEXT.md.

If the built `vlibras.js` bundle is not yet available (user setup required — see `user_setup` frontmatter), create a placeholder file at `web/vlibras/vlibras.js`:
```
// vlibras.js — pre-compiled vlibras-player-webjs bundle
// Build from: https://github.com/spbgovbr-vlibras/vlibras-player-webjs
// Run: npm install && npm run build
// Then replace this file with the build output.
// Expected export: window.VLibras.Player constructor.
console.warn('[vlibras_flutter] vlibras.js placeholder — replace with real bundle before use');
```

This placeholder prevents a 404 on `<script src="/vlibras/vlibras.js"></script>` while logging a warning. The real bundle replaces it.

Check `.gitignore` and ensure `web/vlibras/vlibras.js` is NOT ignored. Add an exception if needed:
```
!web/vlibras/vlibras.js
```

The `target/` directory (Unity WebGL assets) is NOT committed here — it belongs in the app's `web/vlibras/target/` per the CONTEXT.md decision.
  </action>
  <verify>
    <automated>test -f web/vlibras/vlibras.js && echo "OK: vlibras.js exists"</automated>
  </verify>
  <done>
    - web/vlibras/vlibras.js exists (real bundle or placeholder)
    - File is tracked by git (not gitignored)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — avatar renders and translate animates end-to-end</name>
  <what-built>
Complete Phase 3 integration: VLibrasJs bindings, VLibrasWebPlatform with Completer/Timer bridge, VLibrasView with HtmlElementView embedding, VLibrasController conditional import, and vlibras.js in web/vlibras/.

To test end-to-end, first complete user setup:
1. Build vlibras.js from vlibras-player-webjs source (if not done — see user_setup frontmatter)
2. Place Unity WebGL target/ assets at your app's web/vlibras/target/
3. Add &lt;script src="/vlibras/vlibras.js"&gt;&lt;/script&gt; to web/index.html
4. Create a minimal Flutter Web app using VLibrasController + VLibrasView
  </what-built>
  <action>Human runs the app in Chrome browser, verifies avatar rendering and animation, checks browser console for errors, and confirms state transitions via ValueListenableBuilder output.</action>
  <how-to-verify>
Verify these 4 behaviors using a Flutter Web app with the plugin:

**1. Avatar renders (WEB-01)**
- Run `flutter run -d chrome` in a project using the plugin
- Confirm the VLibrasView area shows the VLibras 3D avatar canvas (not blank/black)
- Expected: Unity WebGL loader appears, then avatar is visible

**2. translate() animates (WEB-02)**
- Call `await controller.translate('Ola mundo')` via a button in the test app
- Confirm the avatar performs LIBRAS signing movements
- Expected: avatar animates through the signs, then returns to rest position

**3. State transitions observable (CORE-02)**
- Wrap VLibrasView with a ValueListenableBuilder showing controller.value.status as text
- Observe the sequence: idle -> initializing -> ready -> translating -> playing -> ready
- Expected: all 6 state labels appear in sequence during a full translate cycle

**4. Timeout error fires (CORE-02 error path)**
- Block network requests to /vlibras/target/ in Chrome DevTools Network tab
- Call controller.translate('Ola mundo')
- Wait ~30 seconds
- Expected: controller.value.status becomes VLibrasStatus.error with a timeout message
  </how-to-verify>
  <verify>
    <automated>MISSING — visual verification only; automated assertions cannot confirm Unity WebGL animation</automated>
  </verify>
  <done>User confirms: avatar visible (WEB-01), translate animates (WEB-02), state transitions observable (CORE-02), timeout error fires (CORE-02 error path)</done>
  <resume-signal>
Type "approved" if all 4 behaviors verified, or describe which behaviors failed and what was observed (e.g., "avatar renders but translate does not animate — animation:end never fires").
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification per Task 2 checkpoint.
All 4 behaviors confirmed by user:
- Avatar renders in browser (WEB-01)
- translate() triggers animation (WEB-02)
- State transitions observable via ValueListenableBuilder (CORE-02)
- Timeout error surfaces in VLibrasValue.error (CORE-02 error path)
</verification>

<success_criteria>
- web/vlibras/vlibras.js exists and is committed
- Human confirms: avatar visible, translation animates, states transition correctly, timeout error surfaces
- Phase 3 goal met: "A Flutter Web app can display the VLibras avatar and translate text to LIBRAS end-to-end"
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-platform-integration/03-03-SUMMARY.md`
</output>
