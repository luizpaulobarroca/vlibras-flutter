---
phase: 04-publication-readiness
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04

must_haves:
  truths:
    - "flutter pub publish --dry-run exits 0 with no blocking errors"
    - "flutter test (VM suite) exits 0 with all tests passing"
    - "The example app runs in Chrome and the VLibras avatar is visible"
    - "Dragging the avatar and releasing causes it to animate to the nearest corner"
    - "Typing text and pressing Traduzir causes the avatar to animate"
    - "Status indicator updates as controller transitions through states"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license at project root"
      contains: "MIT License"
    - path: "README.md"
      provides: "Portuguese documentation"
      contains: "Instalação"
    - path: "example/lib/widgets/draggable_avatar.dart"
      provides: "Draggable snap widget"
      contains: "AnimatedPositioned"
  key_links:
    - from: "flutter pub publish --dry-run"
      to: "pub.dev gate"
      via: "All blockers resolved in Plans 01-03"
      pattern: "No issues found"
    - from: "flutter test"
      to: "PUB-04 gate"
      via: "VM suite all passing"
      pattern: "All tests passed"
---

<objective>
Run automated gates and human-verify the complete Phase 4 delivery: confirm flutter pub publish --dry-run passes, all tests pass, and the example app demonstrates the plugin working end-to-end in a real browser.

Purpose: Plans 01-03 produce the artifacts; this plan verifies they actually work together. The dry-run gate catches any lingering publish blockers before the phase is signed off.
Output: Confirmed publishable state + human sign-off on example app UX.
</objective>

<execution_context>
@C:/Users/Luiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-publication-readiness/04-VALIDATION.md
@.planning/phases/04-publication-readiness/04-01-SUMMARY.md
@.planning/phases/04-publication-readiness/04-02-SUMMARY.md
@.planning/phases/04-publication-readiness/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates — test suite and publish dry-run</name>
  <files></files>
  <action>
    Run the following commands in sequence and report results. Fix any failures before proceeding to the checkpoint.

    1. Full VM test suite:
    ```
    flutter test test/vlibras_controller_test.dart test/vlibras_web_platform_test.dart test/vlibras_view_vm_test.dart
    ```
    Expected: All tests pass, exit 0.

    2. Publish dry-run:
    ```
    flutter pub publish --dry-run 2>&1
    ```
    Expected: No blocking errors. Acceptable warnings: informational notes about the package, file counts, size. Unacceptable: "No LICENSE file found", "No README.md", "Error:", failure exit code.

    3. dart doc check:
    ```
    dart doc . 2>&1 | grep -i "warning\|undocumented" | head -20
    ```
    Expected: Zero warnings about undocumented public API.

    If any gate fails, fix the issue (read the failing file, apply the fix, re-run). Do not proceed to the checkpoint until all three pass.
  </action>
  <verify>
    <automated>flutter test test/vlibras_controller_test.dart test/vlibras_web_platform_test.dart test/vlibras_view_vm_test.dart && flutter pub publish --dry-run 2>&1 | tail -5</automated>
  </verify>
  <done>flutter test exits 0. flutter pub publish --dry-run exits 0 with no blocking errors. dart doc reports zero undocumented API warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-04 complete the full Phase 4 delivery:
    - Expanded test suite (VLibrasWebPlatform: idempotent initialize, cancel-and-restart, timeout→error; VLibrasView: non-web branch)
    - Publication metadata: LICENSE (MIT), README.md (Portuguese), CHANGELOG.md (Keep a Changelog), .pubignore, pubspec.yaml (v0.1.0, topics, platforms: web: {})
    - Complete dartdoc on all exported public API
    - Example app at example/ with draggable snap-to-corner VLibrasView, TextField + Traduzir button, and status indicator
  </what-built>
  <how-to-verify>
    Before running the example, copy the VLibras assets into the example app's web/ directory:
    ```
    cp -r web/vlibras example/web/vlibras
    ```

    Then run the example app:
    ```
    cd example && flutter run -d chrome
    ```

    Verify the following behaviors:

    1. **Avatar renders**: The VLibras 3D avatar is visible (floating, roughly 200×200px). It should be in the top-left corner initially.

    2. **Avatar is draggable**: Click and drag the avatar to a different area of the screen. It should follow your cursor.

    3. **Avatar snaps to corner**: Release the avatar mid-screen. It should animate smoothly to the nearest of the 4 corners (top-left, top-right, bottom-left, bottom-right) with a slight overshoot (easeOutBack curve).

    4. **Status indicator**: The status text in the main content area should show "Inicializando..." briefly, then "Pronto" when the player is ready.

    5. **Translation**: Type "Olá mundo" in the text field and press the "Traduzir" button (or press Enter). The status should change to "Traduzindo..." and the avatar should animate the LIBRAS signs.

    6. **UI is polished**: The page has a VLibras branding color (blue #005CA9), a heading, a usage hints card, and the overall layout is presentable as a plugin demo page.

    If any behavior is broken, describe the issue (e.g., "avatar doesn't snap", "button disabled", "black screen") and type "issues: [description]".
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All automated gates must be green before the checkpoint runs:
- `flutter test` (VM suite) exits 0
- `flutter pub publish --dry-run` exits 0 with no blocking errors
- `dart doc .` reports zero undocumented API warnings

Human checkpoint confirms end-to-end behavior in real browser.
</verification>

<success_criteria>
- All automated gates pass (flutter test, publish dry-run, dart doc)
- Human confirms: avatar renders, snaps to corner, translation triggers animation, status indicator updates
- Phase 4 is fully signed off and ready for actual pub.dev publication
</success_criteria>

<output>
After completion, create `.planning/phases/04-publication-readiness/04-04-SUMMARY.md`
</output>
