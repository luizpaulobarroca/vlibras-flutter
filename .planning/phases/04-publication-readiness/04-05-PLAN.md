---
phase: 04-publication-readiness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - .pubignore
autonomous: true
requirements:
  - PUB-03
gap_closure: true

must_haves:
  truths:
    - "pubspec.yaml declares flutter: plugin: platforms: web: {} so pana awards platform support points"
    - "flutter pub publish --dry-run completes with no blocking errors and no dirty-tree warning"
    - "Published package size is not inflated by generated dartdoc HTML"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Plugin platforms declaration for pana scoring"
      contains: "platforms:"
    - path: ".pubignore"
      provides: "Excludes doc/ from published package"
      contains: "doc/"
  key_links:
    - from: "pubspec.yaml"
      to: "pana platform scoring"
      via: "flutter: plugin: platforms: web: {}"
      pattern: "platforms:\\s*\n\\s+web:"
    - from: ".pubignore"
      to: "published package size"
      via: "doc/ exclusion rule"
      pattern: "^doc/"
---

<objective>
Close the two verification gaps blocking full Phase 4 sign-off: add the missing `flutter: plugin: platforms: web: {}` declaration to pubspec.yaml (pana blocker) and add `doc/` to .pubignore (package size warning). Also commit the pending changes in lib/src/platform/web_platform.dart to clear the dirty-tree warning from flutter pub publish --dry-run.

Purpose: Without the platforms declaration, pana scores 0 for platform support. Without doc/ in .pubignore, the published package carries ~225 KB of generated HTML dartdoc. The dirty working tree triggers a "1 checked-in file is modified" warning on every dry-run.
Output: Clean publish state — no pana platform gap, no inflated package, no dirty-tree warning.
</objective>

<execution_context>
@C:/Users/Luiz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luiz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@pubspec.yaml
@.pubignore
@lib/src/platform/web_platform.dart
@.planning/phases/04-publication-readiness/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plugin: platforms: web: {} to pubspec.yaml and add doc/ to .pubignore</name>
  <files>pubspec.yaml, .pubignore</files>
  <action>
    **pubspec.yaml** — The current `flutter:` section is:

    ```yaml
    flutter:
      assets:
        - assets/vlibras.js
    ```

    Replace it with a version that adds the `plugin:` block BEFORE `assets:`:

    ```yaml
    flutter:
      plugin:
        platforms:
          web: {}
      assets:
        - assets/vlibras.js
    ```

    This is the only change to pubspec.yaml. Do not touch version, description, topics, or any other field.

    Note: The CONTEXT.md decision at line 45 explicitly states "plugin.platforms: declarar `web: {}` explicitamente". The 04-02-SUMMARY deliberately omitted it, but that decision contradicts the plan must_have and the CONTEXT.md locked decision. The locked decision wins — add the declaration.

    **.pubignore** — The current .pubignore contains:

    ```
    build/
    spike/
    web/vlibras/target/
    .planning/
    ```

    Append `doc/` so generated dartdoc HTML is not included in the published package. Final .pubignore:

    ```
    build/
    spike/
    web/vlibras/target/
    .planning/
    doc/
    ```

    After writing both files, run:
    ```
    flutter pub publish --dry-run 2>&1
    ```

    Confirm the output no longer shows a dirty-tree warning and that `platforms:` appears in the analysis.
  </action>
  <verify>
    <automated>flutter pub publish --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>pubspec.yaml contains `plugin: platforms: web: {}` nested under `flutter:`. .pubignore contains `doc/` on its own line. flutter pub publish --dry-run shows no "has 1 warning" about modified files and no blocking errors.</done>
</task>

<task type="auto">
  <name>Task 2: Commit pending changes to clear dirty working tree</name>
  <files>lib/src/platform/web_platform.dart</files>
  <action>
    The file lib/src/platform/web_platform.dart has uncommitted changes (the canvas rotation CSS injection fix). This causes flutter pub publish --dry-run to emit "Package has 1 warning: 1 checked-in file is modified in git."

    Stage and commit all pending changes, including the pubspec.yaml and .pubignore edits from Task 1:

    ```
    git add lib/src/platform/web_platform.dart pubspec.yaml .pubignore
    git commit -m "fix(04-pub): add platforms: web: {}, exclude doc/ from package, commit canvas fix"
    ```

    After committing, re-run the dry-run to confirm the dirty-tree warning is gone:
    ```
    flutter pub publish --dry-run 2>&1 | grep -i "warning\|error\|issue" | head -10
    ```

    Expected: No "modified in git" warning. If the publish tool surfaces other warnings (package size, optional metadata), those are acceptable — record them in the task output but do not block completion.
  </action>
  <verify>
    <automated>git status --short && flutter pub publish --dry-run 2>&1 | grep -i "warning\|issue" | head -10</automated>
  </verify>
  <done>git status shows no modified tracked files. flutter pub publish --dry-run emits no "checked-in file is modified" warning. The commit exists with all three files staged together.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the two gaps from VERIFICATION.md are closed:

Gap 1 (was Blocker):
```
grep -A3 "plugin:" pubspec.yaml
```
Expected: Shows `platforms:` and `web: {}` nested under `flutter: plugin:`.

Gap 2 (was Warning):
```
git diff HEAD -- lib/src/platform/web_platform.dart | wc -l
flutter pub publish --dry-run 2>&1 | grep warning
```
Expected: diff is empty (committed), dry-run warning about modified files is gone.

Package size sanity:
```
grep "^doc/" .pubignore
```
Expected: `doc/` appears in .pubignore.
</verification>

<success_criteria>
- pubspec.yaml: `flutter: plugin: platforms: web: {}` block is present
- .pubignore: `doc/` line is present
- git working tree is clean (no modified tracked files)
- flutter pub publish --dry-run has no "checked-in file is modified" warning
- flutter pub publish --dry-run has no blocking errors
- Phase 4 VERIFICATION.md gaps 1 and 2 are both closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-publication-readiness/04-05-SUMMARY.md`
</output>
