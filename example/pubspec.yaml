name: vlibras_flutter_example
description: App de exemplo para o plugin vlibras_flutter.
publish_to: none

environment:
  sdk: '>=3.7.2 <4.0.0'
  flutter: '>=3.7.2'

dependencies:
  flutter:
    sdk: flutter
  vlibras_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
